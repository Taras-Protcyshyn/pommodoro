import React from 'react'
import { Typography } from 'antd'
import moment from 'moment'

const { Title, Paragraph, Text } = Typography;

export const Timer = () => {
  return (
    <Typography>
      <Title>Pommodoro app</Title>
    </Typography>
  )
}